---
let {
	variant = "line",
	class: className,
}: {
	variant?: "line" | "asterisks";
	class?: import("astro/types").HTMLAttributes<"hr">["class:list"];
} = Astro.props;
---

<hr
	class:list={[
		"w-full border-none",
		className,
		{ asterisks: variant === "asterisks" },
	]}
/>
